# サブコマンドプロトコル

> **v5.2**: 自然言語の代わりに構造化されたコマンドで候補を管理します。**複数ID バッチ処理** + **`--force` 強制遷移**をサポートします。

---

## コマンド一覧

| コマンド                    | 説明                     | 後続動作                                                 |
| --------------------------- | ------------------------ | -------------------------------------------------------- |
| `--accept <id> [id2 ...]`   | 候補承認 (1個以上)       | `approved` → `/feature-architect` 自動呼出 → `converted` |
| `--reject <id> [id2 ...]`   | 候補却下 (1個以上)       | `rejected` (事由記録)                                    |
| `--defer <id> [id2 ...]`    | 候補保留 (1個以上)       | `deferred` (再検討時期記録)                              |
| `--list`                    | 候補現況テーブル         | 状態別要約出力                                           |
| `--merge <id> <feature-id>` | 既存Featureに統合 (単一) | `merged` (統合対象記録)                                  |
| `--triage`                  | 待機候補自動分類         | ICE順ソート + 分類提案 + 一括実行                        |

### --force フラグ

`--accept`, `--reject`, `--defer`に `--force`を追加すると**状態検証を迂回**して強制遷移します。

```bash
# 通常: pending_review 状態のみ保留可能
/market-intelligence-scanner --defer ai-error-pattern-tracker

# 強制: converted 等どの状態からでも保留可能 (merged 除外)
/market-intelligence-scanner --force --defer ai-error-pattern-tracker ai-safety-moderation-system
```

**強制遷移規則**:

1. `merged`(Terminal State)からの強制遷移は**不可**
2. 同一状態への遷移は不可 (no-op 防止)
3. ユーザーに**現在の状態 → 対象状態**確認質問必須
4. History `note`に `[FORCE]` 接頭辞自動追加
5. `--force`は `--accept`, `--reject`, `--defer`とのみ結合可能

> **`<id>` 形式**: candidate_idの日付接頭辞なしで kebab-case 部分のみ使用可能
> 例: `minimal-pair-pronunciation-drill` または全体 `2026-01-26-minimal-pair-pronunciation-drill`

### 複数ID バッチ処理

`--accept`, `--reject`, `--defer`は**スペース区切りで複数ID**を受け取れます。

```bash
# 単一処理
/market-intelligence-scanner --accept ai-error-pattern-tracker

# 複数処理
/market-intelligence-scanner --accept ai-error-pattern-tracker ai-safety-moderation-system context-based-phrase-cards
/market-intelligence-scanner --reject drama-comprehension-badge debate-discussion-mode
/market-intelligence-scanner --defer ai-companion-characters speed-quiz-fluency-mode
```

**バッチ実行規則**:

1. 各IDを**順次処理** (順序: 入力順)
2. 個別ID失敗時**該当件のみスキップ**、残りは処理続行 (Partial Success)
3. `--reject`/`--defer` バッチ時**事由/時期は共通適用** (1回のみ質問)
4. `--accept` バッチ時各候補別 `/feature-architect` 呼出は**順次実行**
5. 結果は**バッチ要約テーブル**で出力

> ⚠️ `--merge`は対象Featureが候補ごとに異なるため**単一処理のみサポート**します。

---

## --accept \<id\> [id2 ...]

候補を承認し自動でFeature生成パイプラインを開始します。**複数IDサポート**。

**実行プロトコル**:

```
[単一処理]
1. scan-status.jsonでcandidate検索 (idマッチング)
2. status検証: pending_review または deferred 状態のみ承認可能
   └── --force 時: merged 除外すべての状態許可 (ユーザー確認必須)
3. status → "approved" + history 追加
   └── --force 時: noteに "[FORCE]" 接頭辞追加
4. /feature-architect スキル自動呼出
   └── CONTEXT.json 生成
5. 成功時: status → "converted" + converted_to 記録
6. 結果報告

[バッチ処理]
1. すべてのIDをscan-status.jsonで検証 (存在 + 状態)
   └── --force 時: merged 除外すべての状態有効と判断
2. 有効でないIDはスキップリストに追加 + 警告出力
3. --force 時: 強制遷移対象リスト(現在の状態含む)をユーザーに確認質問
4. 有効な候補を順次処理 (各々上記単一プロトコル1-5実行)
5. バッチ要約テーブル出力
```

**単一出力形式**:

```markdown
✅ 候補承認完了

| 項目                  | 値                                      |
| --------------------- | --------------------------------------- |
| **ID**                | `{candidate-short-id}`                  |
| **候補**              | {candidate_name}                        |
| **ICE Score**         | {score}                                 |
| **生成されたFeature** | {feature-id}                            |
| **CONTEXT**           | docs/features/{feature-id}/CONTEXT.json |

→ 次のステップ: `/feature-spec-generator {NNN}` でSPEC生成
```

**バッチ出力形式**:

```markdown
✅ バッチ承認完了 ({成功}/{全体})

|  #  | ID           | 候補名 |   ICE   |        結果         | Feature      |
| :-: | ------------ | ------ | :-----: | :-----------------: | ------------ |
|  1  | `{short-id}` | {name} | {score} |    ✅ converted     | {feature-id} |
|  2  | `{short-id}` | {name} | {score} |    ✅ converted     | {feature-id} |
|  3  | `{short-id}` | {name} | {score} | ⚠️ スキップ: {事由} | —            |

→ 次のステップ: `/feature-spec-generator` で各SPEC生成
```

---

## --reject \<id\> [id2 ...]

候補を却下します。却下事由をユーザーに質問します。**複数IDサポート**。

**実行プロトコル**:

```
[単一処理]
1. scan-status.jsonでcandidate検索
2. status検証: pending_review または deferred 状態のみ却下可能
   └── --force 時: merged 除外すべての状態許可 (ユーザー確認必須)
3. ユーザーに却下事由質問 (AskUserQuestion 使用):
   - "Japan-Fit 低い"
   - "実装複雑度過大"
   - "優先順位低い"
   - "その他" (直接入力)
4. status → "rejected" + rejection_reason + history 追加
   └── --force 時: noteに "[FORCE]" 接頭辞追加
5. 結果報告

[バッチ処理]
1. すべてのIDをscan-status.jsonで検証 (存在 + 状態)
   └── --force 時: merged 除外すべての状態有効と判断
2. 有効でないIDはスキップリストに追加 + 警告出力
3. --force 時: 強制遷移対象リスト(現在の状態含む)をユーザーに確認質問
4. ユーザーに却下事由を1回のみ質問 (共通適用)
5. 有効な候補を順次処理 (同一事由適用)
6. バッチ要約テーブル出力
```

---

## --defer \<id\> [id2 ...]

候補を保留します。再検討時期をユーザーに質問します。**複数IDサポート**。

**実行プロトコル**:

```
[単一処理]
1. scan-status.jsonでcandidate検索
2. status検証: pending_review 状態のみ保留可能
   └── --force 時: merged 除外すべての状態許可 (ユーザー確認必須)
3. ユーザーに保留事由 + 再検討時期質問 (AskUserQuestion 使用):
   - 事由: "MVP 後検討" / "依存機能未実装" / "その他"
   - 時期: "1ヶ月後" / "3ヶ月後" / "未定"
4. status → "deferred" + deferred_reason + deferred_until + history 追加
   └── --force 時: noteに "[FORCE]" 接頭辞追加
5. 結果報告

[バッチ処理]
1. すべてのIDをscan-status.jsonで検証 (存在 + 状態)
   └── --force 時: merged 除外すべての状態有効と判断
2. 有効でないIDはスキップリストに追加 + 警告出力
3. --force 時: 強制遷移対象リスト(現在の状態含む)をユーザーに確認質問
4. ユーザーに保留事由 + 再検討時期を1回のみ質問 (共通適用)
5. 有効な候補を順次処理 (同一事由/時期適用)
6. バッチ要約テーブル出力
```

---

## --list

現在のすべての候補の状態をテーブルで出力します。

**実行プロトコル**:

```
1. scan-status.json ロード
2. candidates 配列で状態別分類
3. 各候補の短縮ID生成:
   candidate_idから日付接頭辞(YYYY-MM-DD-)除去
   例: "2026-01-26-minimal-pair-pronunciation-drill" → "minimal-pair-pronunciation-drill"
4. 短縮ID含むテーブル出力
```

**出力形式**:

> `ID` カラムには `--accept` 等のコマンドにそのまま使用できる**短縮ID** (日付接頭辞除外)を表示する。

```markdown
## 📋 候補現況

### ⏳ 検討待ち (pending_review)

|  #  | ID                                 | 候補名                           | ICE | J-Fit |   生成日   |
| :-: | ---------------------------------- | -------------------------------- | :-: | :---: | :--------: |
|  1  | `minimal-pair-pronunciation-drill` | Minimal Pair Pronunciation Drill | 7.7 |  10   | 2026-01-26 |
|  2  | `ai-companion-characters`          | AI Companion Characters          | 6.1 |   8   | 2026-01-26 |

### ✅ 承認/変換完了

|  #  | ID                  | 候補名            | 変換Feature           |   変換日   |
| :-: | ------------------- | ----------------- | --------------------- | :--------: |
|  1  | `explain-my-answer` | Explain My Answer | 034-explain-my-answer | 2026-01-26 |

### ⏸️ 保留 / 🔀 統合 / ❌ 却下

|   #    | ID  | 候補名 | 状態 | 事由 |
| :----: | --- | ------ | :--: | ---- |
| (なし) | -   | -      |

---

総候補: {N}個 | 検討待ち: {n1} | 承認: {n2} | 却下: {n3} | 保留: {n4} | 統合: {n5}
```

**⚠️ 必須**: `--list` 出力末尾に必ず以下の「次のステップ」セクションを含める。

**次のステップテンプレート** (複数ID バッチ構文含む必須):

```markdown
### 📌 次のステップ

候補別承認/却下/保留決定 (**単一または複数IDサポート**):

- `/market-intelligence-scanner --accept <id> [id2 ...]` → 承認 + Feature自動生成
- `/market-intelligence-scanner --reject <id> [id2 ...]` → 却下 (共通事由記録)
- `/market-intelligence-scanner --defer <id> [id2 ...]` → 保留 (共通事由/再検討時期)
- `/market-intelligence-scanner --merge <id> <feature-id>` → 既存Featureに統合 (単一のみ)
- `/market-intelligence-scanner --triage` → 自動分類 + Tier別一括実行
```

```bash
# 例示: 複数承認
/market-intelligence-scanner --accept ai-speaking-assessment minimal-pair-pronunciation-drill
# 例示: 複数却下
/market-intelligence-scanner --reject drama-comprehension-badge debate-discussion-mode
# 例示: 複数保留
/market-intelligence-scanner --defer ai-companion-characters speed-quiz-fluency-mode
```

---

## --merge \<candidate-id\> \<feature-id\>

候補を既存Featureに統合します。

**実行プロトコル**:

```
1. scan-status.jsonでcandidate検索
2. docs/features/{feature-id}/ 存在確認
3. status検証: pending_review または deferred 状態のみ統合可能
4. ユーザーに統合メモ質問 (任意)
5. status → "merged" + merged_into + merge_note + history 追加
6. 結果報告
```

---

## --triage

> **v5.0 新規**: pending_review 候補をICE Score順にソートし自動分類を提案します。

**実行プロトコル**:

```
1. scan-status.json ロード
2. pending_review 候補のみフィルタリング
3. ICE Score 降順ソート
4. 自動分類提案:
   - ICE ≥ 8.0 → "✅ 即時承認推奨" (Tier 1)
   - ICE 6.0-7.9 → "🔍 追加調査推奨" (Tier 2)
   - ICE 4.0-5.9 → "📦 保留推奨" (Tier 3)
   - ICE < 4.0 → "❌ 却下推奨" (Tier 4)
5. ユーザーにTier別一括処理可否質問 (AskUserQuestion):
   - "Tier 1 全体承認" / "Tier 3-4 全体保留/却下" / "個別選択" / "キャンセル"
6. ユーザー選択に応じてバッチ実行:
   - Tier 1 承認 → 該当候補を順次 --accept 実行
   - Tier 3 保留 → 該当候補を順次 --defer 実行 (共通事由)
   - Tier 4 却下 → 該当候補を順次 --reject 実行 (共通事由)
7. バッチ要約テーブル出力
```

**出力形式**:

```markdown
## 🔀 Triage 結果

|  #  | ID                            | 候補名                     | ICE  | J-Fit |    提案     |
| :-: | ----------------------------- | -------------------------- | :--: | :---: | :---------: |
|  1  | `ai-error-pattern-tracker`    | AI Error Pattern Tracker   | 10.0 |   9   | ✅ 即時承認 |
|  2  | `ai-safety-moderation-system` | AI Safety & Moderation     | 9.1  |   8   | ✅ 即時承認 |
|  3  | `context-based-phrase-cards`  | Context-Based Phrase Cards | 8.2  |   9   | ✅ 即時承認 |
| ... | ...                           | ...                        | ...  |  ...  |     ...     |

---

Tier 1 (即時承認): {n1}個 | Tier 2 (追加調査): {n2}個
Tier 3 (保留): {n3}個 | Tier 4 (却下): {n4}個

→ Tier別一括処理しますか？
```

**一括実行後出力**:

```markdown
## ✅ Triage 一括処理完了

|   Tier    |  対象  |         処理結果         |
| :-------: | :----: | :----------------------: |
| T1 (承認) | {n1}個 | ✅ {成功}/{n1} converted |
| T3 (保留) | {n3}個 | ⏸️ {成功}/{n3} deferred  |
| T4 (却下) | {n4}個 | ❌ {成功}/{n4} rejected  |

→ pending_review 残余: {n}個
```

---

## エラー処理

| エラー           | メッセージ                                                         | 対処                          |
| ---------------- | ------------------------------------------------------------------ | ----------------------------- |
| ID未発見         | `❌ 候補 '{id}' が見つかりません。--listで確認してください。`      | `--list` 案内                 |
| 不正な状態遷移   | `❌ '{status}' 状態ではこの操作を実行できません。`                 | 許可遷移案内 + `--force` 提案 |
| Feature ID未発見 | `❌ Feature '{id}' が存在しません。`                               | 既存Feature一覧案内           |
| WIP超過          | `⚠️ pending_review {n}個 (制限: 10)。--triageで整理してください。` | `--triage` 案内               |
| バッチ部分失敗   | `⚠️ {成功}/{全体} 処理完了。失敗: {id1}, {id2}`                    | 失敗件別事由表示              |
| バッチ全体失敗   | `❌ 0/{全体} 処理完了。すべてのIDが有効ではありません。`           | `--list` 案内                 |
| --force merged   | `❌ 'merged' はTerminal Stateです。--forceでも遷移できません。`    | 対象Featureで直接処理案内     |
| --force 同一状態 | `❌ すでに '{status}' 状態です。変更不要。`                        | 現在の状態案内                |
