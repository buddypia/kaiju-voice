# Wisdom自動化システム

> **根本的解決策**: 拡張可能な知識管理システム (Wisdom Only アーキテクチャ)

プロジェクトのWisdomシステムを自動管理する統合ツールセットです.

> **Note**: 2026-02-05にWisdom Onlyアーキテクチャに簡素化されました. (参考: `.claude/MIGRATION_PLAN.md`)

---

## 🎯 問題と解決策

### 問題

- **拡張性**: 150個の機能後 Wisdomが50K トークン消費 → セッション毎コスト爆発
- **重複**: 作業間学習共有不可→ 繰り返し学習
- **管理負担**: 手動整理必要 → 時間浪費

### 根本的解決策 (Wisdom Only)

```
Tier 1: HOT (7日以内)        → Wisdom Core (自動ロード)
Tier 2: WARM (8-30日)         → Wisdom Core (自動ロード)
Tier 3: COLD (30日+)          → Archive (必要時ロード)
```

**効果**:

- トークンコスト: 30K → 5K (83% 削減)
- 管理負担: 月1回 → 自動化
- 拡張性: 150機能まで対応

---

## 📦 構成要素

| スクリプト              | 役割                      | 使用時点                    |
| ----------------------- | ------------------------- | --------------------------- |
| `wisdom_ttl_tracker.py` | セクション別参照追跡      | 自動 (バックグラウンド)     |
| `wisdom_splitter.py`    | Core/Feature 分割         | 50個の機能時点              |
| `auto_archive.sh`       | COLD セクションアーカイブ | 月1回                       |
| `wisdom_manager.sh`     | 統合管理インターフェース  | ダッシュボード/メンテナンス |

---

## 🚀 クイック開始

### 1. 初期設定 (1回のみ)

```bash
# 現在Wisdom ファイルでメタデータ生成
.claude/scripts/wisdom_manager.sh init
```

### 2. 現在状態確認

```bash
# ダッシュボード表示
.claude/scripts/wisdom_manager.sh dashboard
```

出力例:

```
📊 全体統計
総セクション: 44
総参照: 67
🔥 HOT: 44 | 🔶 WARM: 0 | ❄️ COLD: 0

📈 分割影響分析
トークン削減: 728 トークン (71.4%)
```

### 3. 健全性チェック

```bash
# サイズ, COLD 比率, トークンコスト確認
.claude/scripts/wisdom_manager.sh health-check
```

---

## 📋 使用シナリオ

### シナリオ 1: 週間モニタリング

```bash
# 毎週月曜日 (自動 or 手動)
.claude/scripts/wisdom_manager.sh dashboard

# 問題発見時
.claude/scripts/wisdom_manager.sh health-check
```

### シナリオ 2: 月間メンテナンス

```bash
# 毎月1日 (自動 or 手動)
.claude/scripts/wisdom_manager.sh maintain
```

自動的に:

1. COLD セクションアーカイブ
2. 統計更新
3. 分割必要有無確認

### シナリオ 3: 50個機能達成 (中期戦略)

```bash
# 分割影響分析
python3 .claude/scripts/wisdom_splitter.py --analyze

# 実際分割実行
.claude/scripts/wisdom_manager.sh split
```

結果:

```
✅ core-patterns.md 生成 (頻繁に参照, 自動ロード)
✅ feature-patterns.md 生成 (たまに参照, 手動ロード)
✅ トークン削減: 71.4%
```

---

## 🔧 高度な使用法

### TTL 追跡 (直接制御)

```bash
# 特定セクション参照記録
python3 .claude/scripts/wisdom_ttl_tracker.py \
  --track "project-patterns.md" "コンポーネントパターン"

# COLD セクションリスト (60日基準)
python3 .claude/scripts/wisdom_ttl_tracker.py --list-cold 60

# 全体統計
python3 .claude/scripts/wisdom_ttl_tracker.py --stats
```

### 分割制御

```bash
# Dry Run (シミュレーションのみ)
python3 .claude/scripts/wisdom_splitter.py --split --dry-run

# 閾値変更 (基本: 5回)
python3 .claude/scripts/wisdom_splitter.py \
  --split --threshold 10

# 分割キャンセル (マージ)
python3 .claude/scripts/wisdom_splitter.py --merge
```

### アーカイブ管理

```bash
# 60日基準アーカイブ
.claude/scripts/auto_archive.sh --archive --ttl-days 60

# アーカイブリスト
.claude/scripts/auto_archive.sh --list

# 復旧
.claude/scripts/auto_archive.sh --restore 20260205_140000
```

---

## 📊 モニタリング指標

### 健全性チェック基準

| 指標                |  正常  |   注意    |  危険  |
| ------------------- | :----: | :-------: | :----: |
| Wisdom サイズ       | <100KB | 100-150KB | >150KB |
| COLD 比率           |  <20%  |  20-40%   |  >40%  |
| トークン/セッション |  <5K   |   5-10K   |  >10K  |

### 段階別目標

| 段階     | 機能個数 | 目標トークン | 対策           |
| -------- | :------: | :----------: | -------------- |
| **短期** |   0-50   |      3K      | モニタリング   |
| **中期** |  50-150  |      5K      | 分割           |
| **長期** |   150+   |      ?       | Vector DB 検討 |

---

## 🔄 自動化設定

### Cron 設定 (オプション)

```bash
# crontab -e

# 週間メンテナンス (毎週月曜日9時)
0 9 * * 1 cd /path/to/project && .claude/scripts/wisdom_manager.sh maintain

# 月間健全性チェック (毎月1日9時)
0 9 1 * * cd /path/to/project && .claude/scripts/wisdom_manager.sh health-check
```

---

## 🐛 トラブルシューティング

### Q: "セクションを見つけることができません"

```bash
# メタデータ再初期化
python3 .claude/scripts/wisdom_ttl_tracker.py --init
```

### Q: "分割後トークンが増えました"

→ Core 閾値が低すぎる. 閾値を上げてください:

```bash
python3 .claude/scripts/wisdom_splitter.py --split --threshold 10
```

### Q: "アーカイブを復旧できません"

```bash
# 使用可能なアーカイブ確認
.claude/scripts/auto_archive.sh --list

# 正しい日付形式で復旧
.claude/scripts/auto_archive.sh --restore YYYYMMDD_HHMMSS
```

---

## 📈 実際効果(予測)

### 50個の機能時点

```
分割前: 80KB Wisdom → 20K トークン/セッション
分割後: 20KB Core + 60KB Feature → 5K トークン/セッション
削減: 75% (15K トークン)
```

### 150個の機能時点

```
分割 + アーカイブ: 120KB → 5K トークン/セッション
削減: Vector DBは不要
```

---

## 🎓 設計原則

1. **根本的解決**: 一時的対策なく拡張可能な構造
2. **自動化優先**: 手動作業最小化
3. **測定可能**: すべての決定はデータ基盤
4. **段階的導入**: 短期 → 中期 → 長期
5. **復旧可能**: すべての変更は取り消し可能

---

## 📚 参考文書

- [Wisdom Only マイグレーション計画](../.claude/MIGRATION_PLAN.md)
- [CLAUDE.md - Wisdom セクション](../../CLAUDE.md#-wisdom-システム)
- [自動化パターン リファレンス](../../docs/oss/README.md)

---

**作成**: 2026-02-05
**バージョン**: 1.0.0
**ライセンス**: MIT
